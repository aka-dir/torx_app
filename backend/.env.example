# Local backend defaults.
# These values are set by START_LOCAL.ps1, but this file documents them.

SKIP_AUTH=1
DEV_GARAGE_ID=local-dev

# Leave these empty for local no-cloud mode.
GCP_PROJECT=
GCS_BUCKET=
FIRESTORE_DATABASE=(default)
